alias c="clear"
alias cat="bat"
alias ls="eza --color"
alias l="ls -lAh"
alias z="zoxide query"
alias compress="xargs -P0 -I % tar -czf %.tar.gz %"

# dnf specific aliases
if command -v dnf >/dev/null 2>&1
then
    source ~/.config/aliases_dnf
fi
